## Plan: Fix Functional Gaps

### Priority 1 — Actually Broken (blocks UX)

#### 1. Add Middleware (`src/middleware.ts`)
- **What**: Supabase SSR middleware for session refresh + route protection
- **How**: Create `middleware.ts` at root with:
  - Session refresh via `supabase-auth-token` cookie handling (standard `@supabase/ssr` pattern)
  - Protected route matcher for `/dashboard`, `/matches`, `/apply`, `/credentials`
  - Redirect unauthed users to `/login` for protected routes
  - Redirect authed users away from `/login`, `/register` to `/dashboard`
- **Why**: Currently no session cookie refresh in server components, no centralized route protection

#### 2. Fix Broken Navbar Links
- **What**: `/browse` and `/success-stories` in Navbar → 404
- **Option A (Recommended)**: Replace `/browse` links with `/matches` (which exists and serves the same discover/browse purpose). Keep `/success-stories` as a placeholder page with "Coming Soon" content.
- **Option B**: Create actual `/browse` page. More work, `/matches` already covers this.
- **Files**: `src/components/Navbar.tsx`, create `src/app/success-stories/page.tsx`

#### 3. Wire Up Shortlisted Page
- **What**: Dashboard shortlisted page is static empty stub
- **How**: Import and call `getSavedProfiles()` from `saved.ts` (server action already exists!), render actual saved profiles with image, name, age, profession, remove button
- **Files**: `src/app/dashboard/shortlisted/page.tsx`

#### 4. Error Boundaries
- **What**: No `error.tsx` or `not-found.tsx` → Next.js yellow/white crash pages
- **How**: Create:
  - `src/app/error.tsx` — generic error boundary, "Something went wrong, try again" with retry button
  - `src/app/not-found.tsx` — custom 404 with consistent design language
  - `src/app/dashboard/error.tsx` — dashboard-specific error boundary
  - `src/app/matches/error.tsx` — matches-specific error boundary
- **Design**: Match warm earth-tone palette, serif headings, include subtle floral decoration

#### 5. Loading States
- **What**: No `loading.tsx` anywhere → blank/white screen during page transitions
- **How**: Create:
  - `src/app/dashboard/loading.tsx` — sidebar-aware skeleton
  - `src/app/matches/loading.tsx` — card grid skeleton
  - `src/app/matches/[id]/loading.tsx` — profile preview skeleton
  - `src/app/dashboard/interests/loading.tsx` — interests list skeleton
  - `src/app/dashboard/messages/loading.tsx` — conversation list skeleton
  - `src/app/dashboard/settings/loading.tsx` — form skeleton
- **Design**: Use subtle pulse animation with warm gray tones, match page structure

#### 6. Navbar Auth State
- **What**: Shows "Log In"/"Create Account" even when logged in
- **How**: Make Navbar async (it's already a server component!) but currently doesn't check auth. Add `supabase.auth.getUser()` call, conditionally render:
  - **Logged out**: Log In / Create Account buttons (current)
  - **Logged in**: Avatar or initials circle + dropdown (Dashboard, Settings, Logout)
- **Files**: `src/components/Navbar.tsx`

#### 7. Notification Toast
- **What**: `NotificationProvider.tsx` has `// TODO: Trigger a visual toast` comment
- **How**: Import `sonner` toast (already in dependencies), call `toast()` when INSERT is received on the notification channel
- **Files**: `src/shared/providers/NotificationProvider.tsx`

---

### Priority 2 — Partially Functional / Tech Debt

#### 8. Fix Compatibility Score in Dashboard
- **What**: `Math.floor(Math.random() * 15) + 80` is fake
- **How**: Use actual `score` from recommendations table (already fetched in `getDashboardData` but not passed through). Map `recommendations` data to include the real score.
- **Files**: `src/app/actions/dashboard.ts`

#### 9. Add Payment Webhook
- **What**: No Razorpay webhook handler for async payment confirmations
- **How**: Create `src/app/api/payment/webhook/route.ts` that verifies webhook signature and activates membership. This prevents race conditions and handles delayed confirmations.
- **Files**: New `src/app/api/payment/webhook/route.ts`

#### 10. Populate Shared Directories
- **What**: `shared/types/`, `shared/constants/`, `services/*/` are empty
- **How**: 
  - `shared/types/index.ts` — export `Database` type re-exports, common app types
  - `shared/constants/index.ts` — export route constants, enum maps
  - Note: Services are intentionally empty (needs real service integrations) — leave as-is, not a functional gap

---

### Files Changed / Created

| File | Action |
|------|--------|
| `src/middleware.ts` | **Create** |
| `src/components/Navbar.tsx` | **Edit** — fix links + add auth-aware state |
| `src/app/success-stories/page.tsx` | **Create** |
| `src/app/dashboard/shortlisted/page.tsx` | **Rewrite** |
| `src/app/error.tsx` | **Create** |
| `src/app/not-found.tsx` | **Create** |
| `src/app/dashboard/error.tsx` | **Create** |
| `src/app/matches/error.tsx` | **Create** |
| `src/app/dashboard/loading.tsx` | **Create** |
| `src/app/matches/loading.tsx` | **Create** |
| `src/app/matches/[id]/loading.tsx` | **Create** |
| `src/app/dashboard/interests/loading.tsx` | **Create** |
| `src/app/dashboard/messages/loading.tsx` | **Create** |
| `src/app/dashboard/settings/loading.tsx` | **Create** |
| `src/shared/providers/NotificationProvider.tsx` | **Edit** — add sonner toast |
| `src/app/actions/dashboard.ts` | **Edit** — pass real compatibility score |
| `src/app/api/payment/webhook/route.ts` | **Create** |
| `src/shared/types/index.ts` | **Create** |
| `src/shared/constants/index.ts` | **Create** |

Total: ~18 files (10 new, 4 edits, 4 rewrites)

### Design Principles
- Match existing warm earth-tone palette (`#FBF9F6`, `#FDF5E6`, `#8C7A6B`, `#2A2621`)
- Use existing CSS variable tokens from `globals.css` where appropriate
- Reuse `cn()` utility from `shared/utils/cn.ts`
- Match existing animation patterns (framer-motion fade-in-up)
- Keep typography consistent (Playfair Display for headings, Inter for body)
